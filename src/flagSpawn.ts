// eslint-disable-next-line max-classes-per-file
import { Actor, Vector } from 'excalibur';

export class FlagSpawn extends Actor {
  constructor(pos: Vector) {
    super({ pos });
  }
}

export class AltFlagSpawn extends Actor {
  constructor(pos: Vector) {
    super({ pos });
  }
}
