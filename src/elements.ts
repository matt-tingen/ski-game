export const canvasContainer = document.getElementById(
  'container',
) as HTMLButtonElement;
export const volumeContainer = document.getElementById(
  'volume-container',
) as HTMLButtonElement;
export const leftTurnButton = document.getElementById(
  'left',
) as HTMLButtonElement;
export const rightTurnButton = document.getElementById(
  'right',
) as HTMLButtonElement;
export const loadingError = document.getElementById('loading-error')!;
